<%# <turbo-stream action="replace" target="<%= dom_id(@suggestion) %1>"> %>
<%#   <template> %>
<%#     <turbo_frame id="<%= dom_id(@suggestion) %1>"> %>
<%#       <li class="w-full px-4 py-2 text-white border-b border-gray-200 h-15 grid grid-cols-6 dark:border-gray-600 bg-gradient-to-r from-teal-500 to-teal-400 rounded-md"> %> 
<%#         <p class="col-span-5"> %>
<%#           <%= @suggestion.description %1> %>
<%#         </p> %>
<%#         <%= button_to "Back", back_suggestion_path(@suggestion.id) %1> %>
<%#       </li> %>
<%#     </turbo_frame> %>
<%#   </template> %>
<%# </turbo-stream> %>

<%# replace with self %>
<%# <%= turbo_stream.replace @suggestion, (render 'suggestion', s: @suggestion) %1> %>
<%# <%= turbo_stream.replace @suggestion, (render 'modifiable', s: @suggestion) %1> %>

<%= turbo_stream.replace @suggestion do %>
  <turbo_frame id="<%= dom_id(@suggestion) %>">
  <li class="w-full px-4 py-2 text-white border-b border-gray-200 h-15 grid grid-cols-6 dark:border-gray-600 bg-gradient-to-r from-teal-500 to-teal-400 rounded-md">
    <p class="col-span-5">
      <%= @suggestion.description %>
    </p>
    <%= button_to "Back", back_suggestion_path(@suggestion.id) %>
  </li>
  </turbo_frame>
<% end %>
